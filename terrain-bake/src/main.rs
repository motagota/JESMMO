//! Offline terrain bake CLI (terrain pipeline epic, issue tracker #56).
//!
//! Scaffold only (#57) — the real CLI shape (`--config`, `--stage`,
//! `--debug-dump`, `--force`) and the synthetic placeholder height source
//! land in #59.

fn main() {
    println!("terrain-bake: scaffold only — see issue #59 for the real CLI");
}
